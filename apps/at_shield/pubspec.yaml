name: at_shield
description: A.T. Shield — bloqueador de sites (Flutter UI)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  at_shield_ui:
    path: ../../packages/at_shield_ui

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
